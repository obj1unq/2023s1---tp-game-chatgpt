import wollok.game.*
import extras.*

program juego {
	game.title("Star Wars Game")
	game.height(15)
	game.width(22)
	mainMenu.iniciar()
	game.start()
}
// TODO: Arreglar el problema de los puntos, una vez que se pasa al siguiente nivel 